'use client';

/**
 * Placeholder for generated Shadcn UI buttons. After `npx shadcn@latest init` (Tailwind + `components/ui`),
 * replace this file with a thin wrapper around `@/components/ui/button` that maps `UiButtonProps` (variant, etc.).
 */
export { Button } from '../default/Button';

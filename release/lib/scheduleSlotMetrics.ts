/**
 * Pixel height of one schedule slot row (time labels, grid, blocks, drag math).
 * Keep aligned with `--schedule-slot-row-height` in `app/globals.css`.
 */
export const SCHEDULE_SLOT_ROW_HEIGHT_PX = 34;

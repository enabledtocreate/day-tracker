{
  "name": "day-tracker/day-tracker",
  "description": "Day Tracker app",
  "require": {
    "php": ">=8.0"
  },
  "require-dev": {
    "phpunit/phpunit": "^10.0"
  },
  "autoload": {
    "files": []
  },
  "scripts": {
    "test": "phpunit",
    "test:run": "phpunit --no-configuration --bootstrap tests/bootstrap.php tests/"
  },
  "config": {
    "allow-plugins": {}
  }
}
